#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
BIN="${TIC80CTL_CLI_BIN:-$SCRIPT_DIR/../build/bin/tic80ctl}"

if [ ! -x "$BIN" ]; then
  printf 'tic80ctl: built binary not found at %s\n' "$BIN" >&2
  exit 1
fi

exec "$BIN" "$@"
